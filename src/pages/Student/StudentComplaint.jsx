import { Layout } from "../../components/student/Layout";
import { Complaint } from "../../components/student/Complaint";

export const StudentComplaint = () => {
  return (
    <Layout content={<Complaint/>}/>
  )
}